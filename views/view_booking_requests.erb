<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>View booking requests</title>
  </head>
  <body>
    <h1>Your booking requests</h1>
    <ul>
      <% @requests.each do |request| %>
        <li><%= request['spacename'] + " has been requested from " + request['startdate'] + " to " + request['enddate'] %></li>
      <% end %>
    </ul>
  </body>
</html>
